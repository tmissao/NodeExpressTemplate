trigger: none

pool: Default

resources:
  pipelines:
    - pipeline: source
      source: merge
      trigger:
        branches:
          - master
          
stages:
  - stage: Deploy
    jobs:
      - job: deploy
        steps:
          - script: echo "deploying -------------> deployed"
      
